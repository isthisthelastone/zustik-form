/**
 * The Valibot version installed as a Zustik Form runtime dependency.
 *
 * Applications may import through `zustik-form/valibot` instead of declaring
 * a separate direct dependency when they only use Valibot for form schemas.
 */
export * from "valibot";
